# Production dependencies for Ubuntu 24.04 (Hostinger VPS).
# Install with: pip install -r requirements/production.txt
# Keep Linux-safe only — no Windows-specific packages (pyautogui, keyboard, etc.).

# Core
Django==4.2.21
djangorestframework==3.15.2
python-decouple==3.8
dj-database-url==2.2.0

# Database (PostgreSQL)
psycopg2-binary==2.9.9

# Media / imaging
Pillow==10.4.0
qrcode==8.2
reportlab==4.4.9

# Background jobs
celery==5.6.3
redis==7.0.1
django-celery-beat==2.9.0

# Optional cloud media (used only if CLOUDINARY_* is set)
cloudinary==1.44.2
django-cloudinary-storage==0.3.0

# Web stack
whitenoise==6.6.0
django-htmx==1.26.0
gunicorn==23.0.0

# HTTP / integrations
requests==2.32.5
